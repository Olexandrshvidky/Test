package variant5.decoraters;

import variant5.AbstractElement;

public abstract class AbstractColorDecorator extends AbstractDecorator {
    public AbstractColorDecorator(AbstractElement abstractElement) {
        super(abstractElement);
    }
}
